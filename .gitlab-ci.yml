---
variables:
  TF_MIN_VERSION: "1.12"
  AZURERM_PROVIDER_MIN_VERSION: "5.0"

include:
  - project: "claranet/projects/cloud/azure/terraform/ci"
    ref: master
    file: "/pipeline.yml"

# Examples skipped temporary until all v9 releases
examples:
  script:
    - exit 0
  rules:
    - when: never

...
